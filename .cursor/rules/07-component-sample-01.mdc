---
alwaysApply: true
---

### 7. ComponentSamples 페이지 구성 (Ant-Design 공식 컴포넌트)

목표:
프로젝트의 모든 컴포넌트 사용 예시 쇼케이스

**꼭 참조해야 할 문서:**
- `.cursor/rules/02-react-antd-rules.mdc`
- `.cursor/rules/03-file-directory.mdc`
- `.cursor/rules/04-common-structures.mdc`


프로젝트의 모든 컴포넌트 사용 예시를 보여주는 라이브러리 문서입니다.
**Ant-Design의 공식 컴포넌트로만** 예시 구성합니다.

## 📌 핵심 규칙: 모든 컴포넌트는 CardForm 구조 사용

**필수 구조:**
- 모든 컴포넌트 예시는 **CardForm (columns={2})** 안에 구성
- Typography, Button, Input, Select, DatePicker, Checkbox, Radio, Switch, Modal, Confirm, Alert, Toast & Notification 모든 섹션 동일하게 적용
- **CardForm.Item 안의 요소들은 반드시 `<Flex gap={8} wrap>`로 감싸기** (공통 구조)
- 일관된 레이아웃으로 샘플 코드 통일성 유지


필수 구성:
1. Typography
   - Title (level 1~5)
   - Text (기본, strong, disabled, 색상 변형)
   - Paragraph

2. Button
   - type: primary, default, dashed, text, link
   - size: large, middle, small
   - danger, ghost, disabled 상태

3. Input
   - 기본 Input (활성/disabled)
   - prefix, suffix 아이콘 (활성/disabled)
   - TextArea (활성/disabled)
   - Password (활성/disabled)

4. Select (single, multiple)
   - 단일 선택 (Single Select - 활성/disabled)
   - 다중 선택 (Multiple Select - 활성/disabled)
   - 검색 가능한 Select (활성/disabled)

5. DatePicker (single, range, time)
   - 단일 날짜 선택 (DatePicker - 활성/disabled)
   - 날짜 범위 선택 (RangePicker - 활성/disabled)
   - 날짜 범위 + 시간 선택 (RangePicker + TimePicker - 활성/disabled)
   - 시간 선택 (TimePicker - 활성/disabled)

6. Upload
   - 기본 Upload (활성/disabled)
   - Upload + 일반 파일 목록
   - Upload + 썸네일 파일 목록

7. Checkbox
   - Checkbox 수평/수직 (활성/disabled)
   - Checkbox Group (활성/disabled)

8. Radio
   - Radio Group 수평/수직 (활성/disabled)
   - Radio Button (활성/disabled)

9. Switch
   - 기본 Switch (활성/disabled)
   - 텍스트 포함 Switch (활성/disabled)

10. Modal (모달)
   - 기본 Modal
   - 푸터 커스텀 Modal

11. Confirm (컨펌)
    - Modal.confirm (확인/취소)
    - Modal.warning (경고)
    - Modal.error (에러)
    - Modal.success (성공)

12. Alert (알럿)
    - type: success, info, warning, error
    - closable (닫기 버튼)
    - 아이콘 포함/미포함

13. Toast & Notification
    - Message (토스트)
      - success, error, warning, info, loading
    - Notification (알림)
      - 위치: topRight, topLeft, bottomRight, bottomLeft
      - type별 예시

14. CardForm (가로 라벨: 1컬럼, 2컬럼 / 세로 라벨: 1컬럼, 2컬럼) - 총 4가지
    **⚠️ 참조 문서 코드 그대로 생성:** `.cursor/rules/08-component-sample-02.mdc`
    **참조 문서의 CardForm 전체 코드를 그대로 복사해서 생성**

    **절대 금지 사항:**
    ❌ label 이름 변경 금지 ("폼 그룹 - 세로 나열 - 텍스트 유" → "이름" 같은 변경 금지)
    ❌ 구조 변경 금지 (CardForm.Item 개수, 내용 변경 금지)
    ❌ 실용적으로 개선하려는 시도 금지

15. CardFilter (세로 라벨, 가로 라벨) - 총 2가지
    **⚠️ 참조 문서 코드 그대로 생성:** `.cursor/rules/08-component-sample-02.mdc`
    **참조 문서의 CardFilter 전체 코드를 그대로 복사해서 생성**

16. CardTable (기본, 필터+테이블, Empty 상태) - 총 3가지
    **⚠️ 참조 문서 코드 그대로 생성:** `.cursor/rules/08-component-sample-02.mdc`
    **⚠️⚠️⚠️ 반드시 참조 문서 코드를 한 글자도 바꾸지 말고 그대로 복사해서 생성**

17. Grid (Grid 안에 Card 구조로 생성)
    - Grid 2컬럼
    - Grid 3컬럼
    - Grid 4컬럼



파일 구성:
- `src/pages/component-samples/ComponentSamples.tsx` (렌더링만)
- `src/pages/component-samples/ComponentSamplesUtil.ts` (샘플 데이터, 옵션)

데이터 분리:
- 필터 옵션 (Select options)
- 폼 필드 정의 (label, placeholder, rules)
- 테이블 컬럼 & 데이터
- 모든 샘플 데이터는 Util.ts에 작성

메뉴 등록:
- AppRouter.tsx에 /component-samples 경로 추가

필수 규칙:
- HTML 태그 절대 사용 금지
- 인라인 스타일 절대 금지
- 데이터/로직은 Util.ts에 분리
- 각 컴포넌트 import해서 사용

생성 후 검증:
- [ ] Typography 섹션 (Title, Text, Paragraph)
- [ ] Button 섹션 (모든 type, size, 상태)
- [ ] Input 섹션 (기본, prefix, suffix, TextArea, Password)
- [ ] Input 섹션 disabled 상태 모두 포함
- [ ] Select 섹션 (단일, 다중, 검색)
- [ ] Select 섹션 disabled 상태 모두 포함
- [ ] DatePicker 섹션 (단일, 범위, 시간)
- [ ] DatePicker 섹션 disabled 상태 모두 포함
- [ ] Upload 섹션 (기본, 일반 파일 목록, 썸네일 파일 목록)
- [ ] Upload 섹션 disabled 상태 포함
- [ ] Checkbox 섹션
- [ ] Checkbox 섹션 disabled 상태 포함
- [ ] Radio 섹션
- [ ] Radio 섹션 disabled 상태 포함
- [ ] Switch 섹션
- [ ] Modal 섹션 (기본, 푸터 커스텀, 중첩)
- [ ] Confirm 섹션 (확인, 경고, 에러, 성공)
- [ ] Alert 섹션 (4가지 type, closable)
- [ ] Toast & Notification 섹션 (Message, Notification)
- [ ] CardForm (가로 라벨: 1컬럼, 2컬럼 / 세로 라벨: 1컬럼, 2컬럼)
- [ ] CardFilter 섹션 (세로 라벨, 가로 라벨)
- [ ] CardTable 섹션 (기본, 필터+테이블, Empty 상태)
- [ ] Grid 섹션 (2/3/4컬럼)
- [ ] 샘플 데이터가 Util.ts에 분리됨
- [ ] HTML 태그 없음

결과 확인:

- [ ] ComponentSamples.tsx 생성됨 (렌더링만)
- [ ] ComponentSamplesUtil.ts 생성됨 (데이터만)
- [ ] 17개 섹션 (25개 세부 카드) 모두 순서대로 표시
- [ ] Typography ~ Grid까지 모든 컴포넌트 예시
- [ ] 사이드바 메뉴에서 클릭 가능
- [ ] 모든 컴포넌트 동작 확인
