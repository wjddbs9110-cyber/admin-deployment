---
alwaysApply: true
---

## 📁 프로젝트 폴더 구조
**아래 폴더 구조 동일하게 구성해야합니다**

```
src/
├── pages/              ← 페이지 컴포넌트 (필수)
│   ├── dashboard/
│   │   ├── Dashboard.tsx
│   │   └── DashboardUtil.tsx
│   └── component-samples/
│       ├── ComponentSamples.tsx
│       └── ComponentSamplesUtil.ts
│
├── components/         ← 공통 컴포넌트 (필수)
│   ├── card/
│   │   ├── common/
│   │   │   └── CardHeader.tsx
│   │   │   └── CardFooter.tsx
│   │   │   └── Pagination.tsx
│   │   │   └── CardType.ts
│   │   ├── CardForm.tsx
│   │   ├── CardFilter.tsx
│   │   ├── CardTable.tsx
│   │   └── CardUtil.ts
│
├── layout/             ← 레이아웃 (필수)
│   ├── Layout01.tsx                # 메인 레이아웃
│   ├── header/
│   │   └── Header.tsx
│   ├── sidebar/
│   │   └── Sidebar.tsx
│   ├── content/
│   │   └── ContentHeader.tsx
│   │   └── ContentBody.tsx
│   │   └── ContentFooter.tsx
│
├── router/             ← 라우팅 (필수)
│   ├── AppRouter.tsx               # 라우터 설정
│   └── AppRouterUtil.tsx           # 메뉴 데이터 + 유틸 함수 (JSX 포함)
│
├── assets/             ← 정적 파일 (필수)
│   ├── css/
│   │   ├── reset.css
│   │   ├── common.css
│   │   ├── layout/
│   │   │   ├── layout.css
│   │   │   ├── header/
│   │   │   │   └── header.css
│   │   │   ├── sidebar/
│   │   │   │   └── sidebar.css
│   │   │   └── content/
│   │   │       └── content.css
│   │   └── components/
│   │       ├── button.css
│   │       ├── card.css
│   │       ├── table.css
│   │       ├── input.css
│   │       ├── select.css
│   │       └── menu.css
│   └── fonts/
│       └── PretendardVariable/       # Pretendard 가변 폰트
│           ├── woff2/
│           │   └── PretendardVariable.woff2
│           └── fonts.css
│
├── App.tsx
└── main.tsx
```
---

## 📝 명시적 Import 규칙

**모든 import는 파일명을 명시해야 합니다.**

### ✅ 올바른 Import 방식

```tsx
// ✅ Util 파일 명시
import { CardForm, CardFilter, CardTable } from "@/components/card/CardUtil";
import { kpiData } from "./DashboardUtil";
import { selectOptions } from "./ComponentSamplesUtil";
import { menuItems } from "@/router/AppRouterUtil";
```

### ❌ 잘못된 Import 방식

```tsx
// ❌ index.ts 사용 금지 (파일 자체를 생성하지 않음)
import { CardForm } from "@/components/card";

// ❌ 폴더 경로만 사용 금지
import { menuItems } from "@/router";
```

### 📋 주요 Util 파일

| 파일 경로 | 용도 | Import 예시 |
|---|---|---|
| `components/card/CardUtil.ts` | 카드 컴포넌트 export | `@/components/card/CardUtil` |
| `pages/dashboard/DashboardUtil.ts` | 대시보드 데이터 | `./DashboardUtil` |
| `pages/component-samples/ComponentSamplesUtil.ts` | 샘플 데이터 | `./ComponentSamplesUtil` |
| `router/AppRouterUtil.tsx` | 메뉴 데이터 + 유틸 | `@/router/AppRouterUtil` |

### 💡 규칙 이유

1. **명확성**: 어떤 파일에서 가져오는지 명확히 표시
2. **일관성**: 프로젝트 전체에서 동일한 패턴 유지
3. **추적성**: 파일 간 의존성 파악 용이
4. **유지보수**: 파일 구조 변경 시 영향 범위 파악 쉬움

---

## 📄 Pretendard 폰트 설정

**GitHub 원본 URL:**
https://github.com/nhnad-deflow/deflow-workspace/tree/50e8845373b3cdb3dfde5718ccea17205e406d63/fonts/PretendardVariable

**중요:**
- `fonts/PretendardVariable/` 디렉토리 전체를 GitHub URL에서 가져올 것
- 폴더 구조와 파일 내용을 GitHub 원본과 동일하게 유지
- `fonts.css`, `woff2/PretendardVariable.woff2` 모두 포함

**절대 생성하지 말 것:**
- ❌ `.gitkeep` 파일
- ❌ `README.md` 파일
- ❌ 기타 더미 파일