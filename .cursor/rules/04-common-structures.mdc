---
alwaysApply: true
---

## 🎯 페이지 컨테이너 구조


**꼭 참조해야 할 문서:**
- `.cursor/rules/02-react-antd-rules.mdc`
- `.cursor/rules/03-file-directory.mdc`
- `.cursor/rules/04-common-structures.mdc`


### 기본 구조
```tsx
import { Space, Typography } from "antd";
import { CardForm, CardFilter, CardTable } from "@/components/card";

const { Title, Text } = Typography;

function PageName() {
    return (
        <Space direction="vertical" size={24}>
            {/* Content-Header */}
            {/* Content-Body */}
            {/* Content-Footer */}
        </Space>
    );
}
```

### 구조 규칙
- **최상위**: `<Space direction="vertical" size={24}>`
- **섹션**: 목적에 맞는 카드 구조 선택
  - **폼 입력**: `CardForm` 사용
  - **필터/검색**: `CardFilter` 사용
  - **데이터 표시**: `CardTable` 사용
- **간격**: `size={24}` 사용하여 24px 간격 적용

---






## 🔘 ButtonGroup 패턴

**목적**: 카드 컴포넌트의 `headerRight`, `footerLeft`, `footerCenter`, `footerRight`에서 재사용할 공통 버튼 그룹

### 기본 패턴

```tsx
// 공통 버튼 그룹 (추가/수정/삭제)
const buttonGroup = (
    <>
        <Button type="primary" icon={<PlusOutlined />}>추가</Button>
        <Button icon={<EditOutlined />}>수정</Button>
        <Button danger icon={<DeleteOutlined />}>삭제</Button>
    </>
);
```

### 사용 예시

```tsx
import { Button } from "antd";
import { PlusOutlined, EditOutlined, DeleteOutlined } from "@ant-design/icons";
import { CardForm } from "@/components/card";

// 공통 버튼 그룹 정의
const buttonGroup = (
    <>
        <Button type="primary" icon={<PlusOutlined />}>추가</Button>
        <Button icon={<EditOutlined />}>수정</Button>
        <Button danger icon={<DeleteOutlined />}>삭제</Button>
    </>
);

// 재사용
<CardForm
    title="사용자 정보"
    headerRight={buttonGroup}
    footerLeft={buttonGroup}
    footerCenter={buttonGroup}
    footerRight={buttonGroup}
>
    {/* 폼 내용 */}
</CardForm>
```

### 장점
- 버튼 그룹을 한 곳에서 정의하여 일관성 유지
- 여러 카드 컴포넌트에서 재사용 가능
- 수정 시 한 곳만 변경하면 됨

---









## 🎴 카드 구조
프로젝트에서 사용하는 3가지 카드 구조입니다.

**공통**
- 카드 구조 `CardHeader.tsx` 생성 시 `className="box-header"` 클래스 부여.
- 카드 구조 `CardFooter.tsx` 생성 시 `className="box-footer"` 클래스 부여.

### 1️⃣ CardTable - 카드 테이블 - 테이블 카드

**목적**: 데이터를 테이블 형태로 표시

**주요 Props**:
- `title`: 카드 제목
- `columns`: 테이블 컬럼 정의
- `dataSource`: 테이블 데이터
- `filterItems`: (선택) 상단 필터 영역
- `pagination`: 페이지네이션 설정
- `showPagination`: 페이지네이션 표시 여부
- `emptyDescription`: 빈 데이터 메시지

**기본 구조**:
```
┌─────────────────────────────────────┐
│ [제목]              [headerRight]   │ ← 헤더
├─────────────────────────────────────┤
│ 검색: [Input] [Button]              │ ← filterItems (선택)
├─────────────────────────────────────┤
│ ┌─────┬─────┬──────┬───────┐       │
│ │이름 │나이 │ 주소 │ 액션  │       │
│ ├─────┼─────┼──────┼───────┤       │ ← Table
│ │홍길동│ 32  │서울시│[수정] │       │
│ └─────┴─────┴──────┴───────┘       │
├─────────────────────────────────────┤
│              [< 1 2 3 >]            │ ← Pagination
└─────────────────────────────────────┘
```

**사용 예시**:
```tsx
<CardTable
    title="사용자 목록"
    columns={[
        { title: "이름", dataIndex: "name", key: "name" },
        { title: "나이", dataIndex: "age", key: "age" }
    ]}
    dataSource={[
        { key: "1", name: "홍길동", age: 32 },
        { key: "2", name: "김철수", age: 28 }
    ]}
    filterItems={[
        { label: "검색", component: <Input placeholder="이름 검색" /> }
    ]}
/>
```

---

### 2️⃣ CardFilter - 카드 필터 - 필터/검색 카드

**목적**: 검색 조건 및 필터링 UI 구성

**주요 Props**:
- `title`: 카드 제목
- `filterItems`: 필터 항목 배열 (label, component)
- `labelLayout`: `"vertical"` 또는 `"horizontal"` (라벨 배치)
- `headerRight`: 헤더 우측 버튼 영역

**기본 구조**:
```
┌─────────────────────────────────────┐
│ [제목]              [headerRight]   │ ← 헤더
├─────────────────────────────────────┤
│ 검색어: [Input]  상태: [Select]     │ ← filterItems
│ [검색 버튼]                          │
└─────────────────────────────────────┘
```

**사용 예시**:
```tsx
<CardFilter
    title="검색 조건"
    filterItems={[
        { label: "검색어", component: <Input placeholder="검색" /> },
        { label: "상태", component: <Select options={[...]} /> },
        { component: <Button type="primary">검색</Button> }
    ]}
/>
```

---

### 3️⃣ CardForm - 카드 폼 - 폼 입력 카드

**목적**: 사용자 입력을 받는 폼 구성

**주요 Props**:
- `title`: 카드 제목
- `columns`: `1` 또는 `2` (단/복수 컬럼)
- `layout`: `"horizontal"` 또는 `"vertical"` (라벨 배치)
- `headerRight`: 헤더 우측 버튼 영역
- `footerLeft`, `footerCenter`, `footerRight`: 푸터 버튼 영역

**기본 구조**:
```
┌─────────────────────────────────────┐
│ [제목]              [headerRight]   │ ← 헤더
├─────────────────────────────────────┤
│ Label 1: [Input]                    │
│ Label 2: [Select]                   │ ← 본문 (CardForm.Item)
│ Label 3: [DatePicker]               │
├─────────────────────────────────────┤
│ [footerLeft] [footerCenter] [footerRight] │ ← 푸터
└─────────────────────────────────────┘
```

**사용 예시**:
```tsx
<CardForm
    title="사용자 정보"
    columns={2}
    layout="horizontal"
    headerRight={buttonGroup}
>
    <CardForm.Item label="이름">
        <Input placeholder="이름 입력" />
    </CardForm.Item>
    <CardForm.Item label="이메일">
        <Input placeholder="이메일 입력" />
    </CardForm.Item>
</CardForm>
```

---

## 📋 컴포넌트 선택 가이드

| 용도 | 컴포넌트 | 예시 |
|------|---------|------|
| 데이터 조회 | `CardTable` | 목록, 통계, 히스토리 |
| 검색/필터 | `CardFilter` | 검색 조건, 날짜 범위, 카테고리 필터 |
| 데이터 입력 | `CardForm` | 회원가입, 설정 변경, 게시글 작성 |

**상세 구현 코드**: `.cursor/rules/08-component-sample-02.mdc` 참조

---